import { promessas } from './promessas.js';
const active=promessas.filter(p=>p.ativo);const $=id=>document.getElementById(id);const BIBLE='https://biblia.midasstudio.com.br';const SAVED='vereda-promessas-saved';let current=active.find(p=>p.id==='psa-121-7')||active[0],locked=false;
const readSaved=()=>{try{return JSON.parse(localStorage.getItem(SAVED)||'[]')}catch{return[]}};const writeSaved=v=>localStorage.setItem(SAVED,JSON.stringify(v));
function render(){ $('promise-category').textContent=(current.categoria||'PROMESSA').toUpperCase();$('promise-text').textContent=current.texto;$('promise-reference').textContent=current.referencia;$('promise-translation').textContent=current.traducao||''; }
function paperSound(){try{const C=window.AudioContext||window.webkitAudioContext,c=new C(),b=c.createBuffer(1,c.sampleRate*.38,c.sampleRate),d=b.getChannelData(0);for(let i=0;i<d.length;i++)d[i]=(Math.random()*2-1)*(1-i/d.length)*.08;const s=c.createBufferSource(),f=c.createBiquadFilter();f.type='bandpass';f.frequency.value=1400;s.buffer=b;s.connect(f).connect(c.destination);s.start()}catch{}}
function next(){if(locked||active.length<2)return;locked=true;paperSound();const pool=active.filter(p=>p.id!==current.id);current=pool[Math.floor(Math.random()*pool.length)];render();setTimeout(()=>locked=false,500)}
function save(){const ids=readSaved(),has=ids.includes(current.id);writeSaved(has?ids.filter(id=>id!==current.id):[current.id,...ids]);alert(has?'Promessa removida':'Promessa guardada')}
function shareText(){return `“${current.texto}”\n\n${current.referencia}\n\nVEREDA · Promessas de Deus`}
async function share(){try{if(navigator.share)await navigator.share({title:'VEREDA | Promessas de Deus',text:shareText()});else{await navigator.clipboard.writeText(shareText());alert('Promessa copiada')}}catch{}}
function saved(){const list=$('saved-list');const items=readSaved().map(id=>active.find(p=>p.id===id)).filter(Boolean);list.innerHTML=items.length?items.map(p=>`<p><b>${p.referencia}</b><br>${p.texto}</p>`).join(''):'Nenhuma promessa guardada ainda.';$('saved-sheet').showModal()}
$('new-promise').addEventListener('click',next);$('save-promise').addEventListener('click',save);$('share-promise').addEventListener('click',share);$('read-bible').addEventListener('click',()=>location.href=`${BIBLE}/#/leitura/${current.bookId}/${current.capitulo}/${current.versiculoInicial}`);document.querySelector('[data-nav="guardadas"]').addEventListener('click',saved);document.querySelector('[data-nav="compartilhar"]').addEventListener('click',share);document.querySelector('[data-nav="biblia"]').addEventListener('click',()=>location.href=`${BIBLE}/#/inicio`);document.querySelectorAll('[data-close]').forEach(b=>b.addEventListener('click',()=>$(b.dataset.close).close()));$('native-share').addEventListener('click',share);$('copy-share').addEventListener('click',async()=>{await navigator.clipboard.writeText(shareText());alert('Texto copiado')});render();